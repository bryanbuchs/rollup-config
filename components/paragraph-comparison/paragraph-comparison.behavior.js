/**
 * Paragraph/Comparison
 * Where to start table
 */

Drupal.behaviors.ParagraphComparison = {
  attach: function (context, settings) {
    Tablesaw.init()
    // const elements = once('ParagraphComparison', '.paragraph-comparison', context)
    // elements.forEach((paragraph) => {
    //   // @TODO: ???
    // })
  }
}
